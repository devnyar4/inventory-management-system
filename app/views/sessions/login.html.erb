<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title>在庫管理システム</title>
  <meta name="description" content="">
  <meta name="author" content="">
</head>
<body>
  <main>
  <div class="container" style="max-width: 500px; margin-top: 100px;">
    <%= form_with url: login_path, method: :post, local: true, data: { turbo: false } do |f| %>
      <!-- ログインID -->
      <div class="row align-items-center mb-4">
        <div class="col-3">
          <%= f.label :login_id, 'ログインID', class: 'form-label' %>
        </div>
        <div class="col-9">
          <%= f.text_field :login_id, class: 'form-control', maxlength: 10 %>
        </div>
      </div>
      <!-- パスワード -->
      <div class="row align-items-center mb-5">
        <div class="col-3">
          <%= f.label :password, 'パスワード', class: 'form-label' %>
        </div>
        <div class="col-9">
          <%= f.password_field :password, class: 'form-control', maxlength: 32 %>
        </div>
      </div>
      <!-- ログインボタン -->
      <div class="row mb-4">
        <div class="col-12">
          <%= f.submit 'ログイン', class: 'btn btn-primary w-100' %>
        </div>
      </div>
    <% end %>

    <!-- エラーメッセージ -->
    <% if flash[:alert] %>
      <div class="alert alert-danger mt-4" role="alert">
        <%= flash[:alert] %>
      </div>
    <% end %>
  </div>
</main>
</body>
</html>